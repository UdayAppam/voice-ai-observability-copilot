# Backend API base URL for local development
# In production (same-origin on Railway), this is not set — falls back to /api
VITE_API_BASE_URL=http://localhost:3001/api

# API key must match backend API_KEY env var
VITE_API_KEY=test-api-key-123
